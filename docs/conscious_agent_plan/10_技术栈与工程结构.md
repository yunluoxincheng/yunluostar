# 10. 技术栈与工程结构

> **方向说明：** yunluostar 已从 CLI-first agent 转型为多平台认知型 Bot 平台。CLI 保留为 admin/debug/developer 工具。

## 1. MVP 技术栈

推荐：

- TypeScript + Node.js 20+
- 多平台认知型 Bot 平台
- 当前 Node.js 原生 HTTP runtime；Hono 可作为后续可选重构方向
- Zod-validated Bot message protocol
- SQLite
- sqlite-vec
- OpenAI / Claude / Qwen / Llama 等 LLM API
- Drizzle ORM
- Commander (CLI admin/debug 工具)
- Vitest

## 2. 进阶技术栈

- IM 平台适配器（QQ、Telegram、Discord、Slack、飞书、钉钉、企业微信）
- Web Dashboard
- 插件市场
- PostgreSQL
- Qdrant / Chroma / Milvus
- Docker / Kubernetes 部署
- 独立 coding agent 产品（与 Bot 平台分离）

## 3. 工程目录结构

```text
src/
  bot/                    # Bot 协议类型、scope 映射、adapter 接口
  adapters/               # Bot 适配器实现
    http-webhook/         # HTTP webhook 适配器
  plugins/                # 插件 manifest、registry、lifecycle、hooks
  runtime/                # Bot Runtime（agent controller、LLM、embedding、memory）
  runtime-client/         # 客户端层（CLI 访问 runtime 的唯一路径）
  protocol/               # Zod-validated 协议 schema
  cli/                    # CLI admin/debug 入口（不是主产品入口）
  agent/
    controller.ts
    input_interpreter.ts
    cognitive_integrator.ts
  memory/
    memory_store.ts
    memory_awakener.ts
    memory_consolidator.ts
    episodic_memory.ts
    semantic_memory.ts
    self_memory.ts
  models/
    working_memory.ts
    self_model.ts
    user_model.ts
    goal.ts
    world_model.ts
  planning/
    planner.ts
    action_executor.ts
    plan_evaluator.ts
  metacognition/
    monitor.ts
    reflector.ts
  db/
    schema.ts
    migrations/
    repositories/
  llm/
    client.ts
    prompts.ts
    schemas.ts
tests/
scripts/
```

## 4. Agent Controller 伪代码

```ts
export async function handleUserInput(userInput: string, sessionId: string) {
  const workingMemory = await loadWorkingMemory(sessionId);

  const intent = await inputInterpreter.parse(userInput);
  workingMemory.updateFromIntent(intent);

  const recalledMemories = await memoryAwakener.retrieve({
    userInput,
    workingMemory,
  });

  const cognitiveState = await cognitiveIntegrator.integrate({
    workingMemory,
    memories: recalledMemories,
    selfModel: await loadSelfModel(),
    userModel: await loadUserModel(),
  });

  const goal = await goalManager.selectGoal(cognitiveState);
  const plan = await planner.createPlan(goal, cognitiveState);

  const metacognitionResult = await monitor.check(plan, cognitiveState);
  const response = await llm.generateResponse(plan, cognitiveState, metacognitionResult);

  const episode = await episodeRecorder.record(userInput, response, cognitiveState);
  const reflection = await reflector.reflect(episode);
  await memoryConsolidator.consolidate(episode, reflection);

  await saveWorkingMemory(workingMemory);
  return response;
}
```
